---
phase: 12-audit-log
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - src/lib.rs
  - src/config.rs
  - src/audit/mod.rs
  - src/audit/types.rs
  - src/audit/error.rs
  - src/audit/loader.rs
  - examples/sign_audit.rs
  - audit_log.json
autonomous: true
requirements:
  - AUDIT-01
  - AUDIT-02
  - AUDIT-03
  - AUDIT-06

must_haves:
  truths:
    - "AuditEntry struct has all required fields: id, timestamp, action, category, data, actor, signature, previous_hash"
    - "AuditAction enum includes all action types including future ones (identity_registered, proposal_submitted, etc.)"
    - "Loader verifies Ed25519 signatures at load time using canonical format and fails on invalid signatures"
    - "audit_log.json contains exactly 40 signed entries: 10 category_added then 30 source_added"
    - "Each entry has a valid previous_hash linking to the prior entry (null for genesis)"
  artifacts:
    - path: "src/audit/types.rs"
      provides: "AuditEntry, AuditAction, AuditFilterParams structs"
      contains: "pub struct AuditEntry"
    - path: "src/audit/loader.rs"
      provides: "load() function with signature verification"
      exports: ["load"]
    - path: "src/audit/error.rs"
      provides: "AuditError enum with thiserror"
      contains: "pub enum AuditError"
    - path: "src/audit/mod.rs"
      provides: "Module re-exports"
      exports: ["AuditEntry", "AuditAction", "AuditError", "load"]
    - path: "audit_log.json"
      provides: "40 retroactive audit entries"
      min_lines: 50
    - path: "src/config.rs"
      provides: "AUDIT_LOG_PATH config field"
      contains: "audit_log_path"
  key_links:
    - from: "src/audit/loader.rs"
      to: "src/audit/types.rs"
      via: "imports AuditEntry, AuditAction"
      pattern: "use super.*AuditEntry"
    - from: "src/audit/loader.rs"
      to: "ed25519-dalek"
      via: "signature verification"
      pattern: "VerifyingKey.*verify"
    - from: "examples/sign_audit.rs"
      to: "src/audit/types.rs"
      via: "uses same AuditEntry type for serialization"
      pattern: "use three_good_sources::audit"
---

<objective>
Create the audit module foundation and generate the signed audit log data file.

Purpose: Establish the core audit log types, cryptographic verification logic, and the initial 40-entry signed audit log (10 categories + 30 sources from v1.0). This is the data foundation that the REST endpoint and MCP tool will serve.

Output: src/audit/ module (4 files), audit_log.json with 40 signed entries, signing utility in examples/
</objective>

<execution_context>
@/home/john/.claude/get-shit-done/workflows/execute-plan.md
@/home/john/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-audit-log/12-CONTEXT.md
@.planning/phases/12-audit-log/12-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/registry/types.rs (pattern to mirror):
```rust
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(deny_unknown_fields)]
pub struct Registry { ... }
```

From src/registry/loader.rs (pattern to mirror):
```rust
pub async fn load(path: impl AsRef<Path>) -> Result<Registry, RegistryError> {
    let contents = fs::read_to_string(path).await...;
    let registry: Registry = serde_json::from_str(&contents)...;
    validate(&registry)?;
    Ok(registry)
}
```

From src/registry/error.rs (pattern to mirror):
```rust
#[derive(Debug, Error)]
pub enum RegistryError {
    #[error("Failed to read registry file at {path}: {error}")]
    FileRead { path: String, error: String },
    ...
}
```

From src/config.rs (add audit_log_path alongside registry_path):
```rust
#[derive(Debug, Deserialize)]
pub struct Config {
    pub registry_path: PathBuf,
    pub log_format: String,
    pub port: u16,
    pub pkarr_secret_key: Option<String>,
}
```

From Cargo.toml (current dependencies -- add new ones):
```toml
[dependencies]
hex = "0.4"
pkarr = { version = "5.0", default-features = false, features = ["keys"] }
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.149"
thiserror = "2.0.18"
```

From registry.json: 10 categories (bitcoin-node-setup, self-hosted-email, rust-learning, home-automation-private, password-management, linux-hardening, threat-modeling, nostr-development, pubky-development, mcp-development), 30 sources (3 per category).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create audit module with types, error, and loader</name>
  <files>Cargo.toml, src/lib.rs, src/config.rs, src/audit/mod.rs, src/audit/types.rs, src/audit/error.rs, src/audit/loader.rs</files>
  <behavior>
    - Test: AuditEntry deserializes from JSON with all required fields (id, timestamp, action, category, data, actor, signature, previous_hash)
    - Test: AuditAction serializes to snake_case strings (source_added, category_added, etc.)
    - Test: AuditAction includes future types (identity_registered, identity_updated, proposal_submitted, proposal_status_changed, vote_cast)
    - Test: canonical_message produces deterministic output for same input (timestamp|action|category|sha256(data_json)|actor_pubkey)
    - Test: verify_signature succeeds for valid Ed25519 signature over canonical message
    - Test: verify_signature fails for tampered data (modified action or data)
    - Test: loader rejects JSON with invalid signature (AuditError::SignatureVerificationFailed)
    - Test: loader accepts valid audit log JSON and returns Vec<AuditEntry>
    - Test: AuditEntry uses #[serde(default)] not deny_unknown_fields (extra fields in JSON don't cause parse failure)
    - Test: category field is Option<String> -- null deserializes correctly
    - Test: chrono timestamp uses Z suffix in canonical format (to_rfc3339_opts with SecondsFormat::Secs, use_z=true)
  </behavior>
  <action>
    1. Add dependencies to Cargo.toml:
       - `ed25519-dalek = { version = "=3.0.0-pre.6", features = ["serde"] }` (pin exact version per RESEARCH pitfall 1)
       - `sha2 = "=0.11.0-rc.5"` (pin exact version to match pkarr's transitive dep)
       - `uuid = { version = "1", features = ["v4", "serde"] }`
       - `chrono = { version = "0.4", features = ["serde"] }`

    2. Create src/audit/types.rs:
       - AuditEntry struct with: id (Uuid), timestamp (DateTime<Utc>), action (AuditAction), category (Option<String>), data (serde_json::Value), actor (String -- hex-encoded 32-byte pubkey), signature (String -- hex-encoded Ed25519 sig), previous_hash (Option<String> -- hex-encoded SHA-256, null for genesis)
       - Use `#[serde(default)]` on AuditEntry (NOT deny_unknown_fields -- per user decision for schema evolution)
       - AuditAction enum with `#[serde(rename_all = "snake_case")]`: SourceAdded, SourceUpdated, SourceRemoved, CategoryAdded, CategoryUpdated, CategoryRemoved, IdentityRegistered, IdentityUpdated, ProposalSubmitted, ProposalStatusChanged, VoteCast
       - AuditFilterParams struct with Deserialize: since (Option<String>), category (Option<String>), action (Option<String>)
       - `pub fn canonical_message(entry: &AuditEntry) -> String` -- format: `{timestamp}|{action}|{category_or_empty}|{sha256_of_data_json}|{actor}`. Use `entry.timestamp.to_rfc3339_opts(chrono::SecondsFormat::Secs, true)` for Z suffix (RESEARCH pitfall 5). Use `serde_json::to_string(&entry.data)` for data hash (BTreeMap ordering is deterministic). Use empty string for None category.
       - `pub fn hash_entry_json(entry: &AuditEntry) -> String` -- SHA-256 of compact JSON serialization of the full entry, hex-encoded. Used for previous_hash chain.

    3. Create src/audit/error.rs:
       - AuditError enum with thiserror derive, matching registry error pattern:
         - FileRead { path, error }
         - JsonParse { path, error }
         - InvalidActorKey { id: Uuid }
         - InvalidSignature { id: Uuid }
         - SignatureVerificationFailed { id: Uuid }

    4. Create src/audit/loader.rs:
       - `pub async fn load(path: impl AsRef<Path>) -> Result<Vec<AuditEntry>, AuditError>`
       - Read file with tokio::fs::read_to_string
       - Parse as Vec<AuditEntry> with serde_json
       - For each entry: call verify_signature() -- decode actor hex to 32 bytes -> VerifyingKey, decode signature hex to 64 bytes -> Signature, build canonical_message, call verifying_key.verify(message.as_bytes(), &signature)
       - Log success with tracing::info including entry count
       - Private fn verify_signature(entry: &AuditEntry) -> Result<(), AuditError>

    5. Create src/audit/mod.rs:
       - pub mod error, loader, types
       - pub use error::AuditError, loader::load, types::{AuditEntry, AuditAction, AuditFilterParams, canonical_message, hash_entry_json}

    6. Update src/lib.rs: add `pub mod audit;`

    7. Update src/config.rs: add `pub audit_log_path: PathBuf` field to Config struct (loaded from AUDIT_LOG_PATH env var, required like registry_path)

    IMPORTANT: For ed25519-dalek Signer trait -- import `use ed25519_dalek::Signer;` to bring try_sign into scope (RESEARCH open question 3). For verification, import `use ed25519_dalek::Verifier;`.
  </action>
  <verify>
    <automated>cargo test --lib audit -- --nocapture</automated>
  </verify>
  <done>
    - src/audit/ module exists with 4 files matching registry pattern
    - All unit tests pass: deserialization, canonical format, signature verification (valid + invalid)
    - cargo build succeeds with new dependencies
    - Config has audit_log_path field
  </done>
</task>

<task type="auto">
  <name>Task 2: Create signing utility and generate audit_log.json with 40 retroactive entries</name>
  <files>examples/sign_audit.rs, audit_log.json</files>
  <action>
    1. Create examples/sign_audit.rs -- a Cargo example binary that:
       - Reads PKARR_SECRET_KEY from env (64-char hex -> 32 bytes)
       - Reads registry.json to extract all 10 categories and 30 sources
       - Generates 40 AuditEntry structs:
         - First 10: category_added entries (one per category, ordered alphabetically by slug)
         - Next 30: source_added entries (grouped by category alphabetical slug, sources ordered by rank within each category)
       - All entries timestamped 2026-02-03T00:00:00Z (v1.0 ship date per user decision)
       - Each entry gets a UUID v4 id
       - For category_added: category = Some(slug), data = json with category name and description
       - For source_added: category = Some(slug), data = json with source name, url, type, rank, why
       - action field uses AuditAction::CategoryAdded / AuditAction::SourceAdded
       - First entry (genesis): previous_hash = None
       - All subsequent entries: previous_hash = Some(hash_entry_json(&previous_entry))
       - Signs each entry: build canonical_message, sign with ed25519_dalek::SigningKey, hex-encode signature, set actor to hex-encoded verifying key bytes
       - Serializes Vec<AuditEntry> to pretty JSON and writes to audit_log.json

    2. Run the signing utility to generate audit_log.json:
       - Need the project's PKARR_SECRET_KEY. Check .env file for it, or if unavailable, the example should generate a deterministic test key and print a warning.
       - `cargo run --example sign_audit` to produce audit_log.json at project root

    3. Verify the generated file:
       - Confirm 40 entries in the JSON array
       - Confirm first 10 are category_added, next 30 are source_added
       - Confirm genesis entry has previous_hash: null
       - Confirm all other entries have non-null previous_hash
       - Run `cargo test --lib audit` to verify the loader can load and verify all signatures in the generated file

    IMPORTANT: The signing script uses the same canonical_message and hash_entry_json functions from src/audit/types.rs (import via `use three_good_sources::audit::{...}`). This ensures signing and verification use identical logic.

    NOTE: Sort categories alphabetically by slug to produce deterministic ordering. Within each category, sources are ordered by rank (1, 2, 3).
  </action>
  <verify>
    <automated>cargo run --example sign_audit && python3 -c "import json; d=json.load(open('audit_log.json')); assert len(d)==40, f'Expected 40, got {len(d)}'; cats=[e for e in d if e['action']=='category_added']; assert len(cats)==10; srcs=[e for e in d if e['action']=='source_added']; assert len(srcs)==30; assert d[0]['previous_hash'] is None; print('OK: 40 entries, 10 categories, 30 sources, genesis has null previous_hash')"</automated>
  </verify>
  <done>
    - examples/sign_audit.rs exists and runs successfully
    - audit_log.json at project root contains exactly 40 entries
    - 10 category_added entries followed by 30 source_added entries
    - Genesis entry has previous_hash: null, all others have valid previous_hash
    - All entries have valid Ed25519 signatures (verified by loader tests)
    - All entries timestamped 2026-02-03T00:00:00Z
  </done>
</task>

</tasks>

<verification>
- `cargo build` succeeds with no warnings
- `cargo test --lib audit` passes all unit tests
- `cargo run --example sign_audit` produces valid audit_log.json
- audit_log.json has 40 entries with correct structure
</verification>

<success_criteria>
- src/audit/ module with types, loader, error, mod mirrors the registry pattern
- Ed25519 signature verification works at load time
- audit_log.json has 40 signed, hash-chained retroactive entries
- Config struct accepts AUDIT_LOG_PATH
</success_criteria>

<output>
After completion, create `.planning/phases/12-audit-log/12-01-SUMMARY.md`
</output>
